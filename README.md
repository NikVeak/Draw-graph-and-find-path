# Draw-graph-and-find-path

double-l-click -  add node
l-click one node, -click other node - add edge
l-click node - change node
l-click edge - change edge
delete + n - delete node
delete + e - delete edge
